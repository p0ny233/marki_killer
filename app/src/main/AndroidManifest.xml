<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.hook"
    >

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".HookEntry"
            android:exported="true"
            tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- 1. 是否为Xposed模块 -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />

        <!-- 2. 模块最低支持的Api版本 一般填82即可 -->
        <meta-data
            android:name="xposedminversion"
            android:value="82" /> <!-- 使用你模块所需的最低版本号 -->

        <!-- 3. 模块的简介（在框架中显示） -->
        <meta-data
            android:name="xposeddescription"
            android:value="马克水印相机模块v0.0.1(2026-06-18), 作用于马克水印相机15.7.1版本,修改指定任意时间"/>

        <!-- 4. 模块作用域 (指定 推荐哪些App可以应用到该模块)-->
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposedscope" />  <!-- 引用 app\src\main\res\values\arrays.xml 文件的 string-array-->

    </application>

</manifest>